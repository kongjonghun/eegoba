<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lgcns.eegoba.api.user.mapper.UserMapper">

    <insert id="insertUser" parameterType="UserVO">
        INSERT INTO TB_USER (
            USER_NAME,
            EMAIL,
            GENDER,
            NICKNAME,
            PASSWORD,
            BIRTHDATE,
            POSITION
        ) VALUES (
            #{userName},
            #{email},
            #{gender},
            #{nickname},
            #{password},
            #{birthdate},
            #{position}
        )
    </insert>

    <select id="getUserByEmail" resultType="UserVO">
        SELECT
            USER_ID,
            USER_NAME,
            EMAIL,
            GENDER,
            NICKNAME,
            PASSWORD,
            BIRTHDATE,
            POSITION
        FROM TB_USER
        WHERE EMAIL = #{email}
    </select>

    <update id="updatePasswordByEmail" parameterType="UserPasswordUpdateVO">
        UPDATE TB_USER
        SET PASSWORD = #{password}
        WHERE EMAIL = #{email}
    </update>

</mapper>